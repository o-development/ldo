<% const interfaces = typings.filter(t => t.dts.kind === "interface"); -%>
<% const imports = typings.filter(t => t.dts.kind.startsWith("import")); -%>
import type { LdoJsonldContext, LdSet } from "@ldo/ldo";
<% imports.forEach((imp) => { -%>
<%- imp.typingString -%>
<% }) -%>

/**
 * =============================================================================
 * Typescript Typings for <%- fileName %>
 * =============================================================================
 */

<% interfaces.forEach((typing) => { -%>
/**
 * <%- typing.dts.name %> Type
 */
export <%- typing.typingString -%>
<% }); -%>
